extends layout


block content
  script(src="https://swww.tokbox.com/webrtc/v2.0/js/TB.min.js")
  script
    var sessionId = "#{sessionId}"
    var apiKey = #{OpenTokKey}
    var token = "#{token}"
  script(src="/javascripts/main.js")
  h1 #{title}
  p= OpenTokKey
  p= sessionId
  p= token
  a(href="/about") something
  div#myPublisherDiv
